/* eslint no-unused-vars: 0 */

// This is the default mapping relating each abstract resource with
// the concrete implementation.
var mapping = {
  'controller': 'actual-controller.js',
  'utils/dialogs': 'actual-dialogs.js'
};
